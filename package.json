{
  "name": "crypto-pricing",
  "version": "0.0.2",
  "author": "ℭ𝔦𝔭𝔥𝔢𝔯",
  "description": "aggregate cryptocurrency pricing utility",
  "scripts": {
    "clean:build": "rm -ir .wrangler dist",
    "clean:all": "rm -ir .wrangler dist node_modules",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "build": "bun build ./src/index.ts --target bun --outfile dist/worker.js",
    "start": "bunx wrangler dev",
    "cf-typegen": "wrangler types",
    "format": "dprint fmt",
    "format:check": "dprint check",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "devDependencies": {
    "wrangler": "^4.33.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "@scalar/hono-api-reference": "^0.9.16",
    "@types/bun": "^1.2.21",
    "dprint": "^0.50.1",
    "hono": "^4.9.4",
    "openapi3-ts": "^4.5.0",
    "typescript": "^5.9.2",
    "zod": "^4.1.3"
  }
}
